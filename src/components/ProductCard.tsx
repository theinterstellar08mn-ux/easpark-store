import { Heart, Star } from "lucide-react";
import { Link } from "react-router";
import type { Product } from "../data/products";

export function ProductCard({ product, saved, onToggleSaved, onAddToCart, dark = false }: { product: Product; saved: boolean; onToggleSaved: (id: number) => void; onAddToCart: (product: Product) => void; dark?: boolean }) {
  return <article className="group relative"><div className={`relative overflow-hidden ${dark ? "bg-white/10" : "bg-[#eeeae4]"}`}><Link to={`/product/${product.id}`} className="block"><img src={product.image} alt={product.name} loading="lazy" className="aspect-[4/5] w-full object-cover transition duration-500 group-hover:scale-105" /></Link>{product.badge && <span className="absolute left-2 top-2 bg-[#ff6b00] px-2 py-1 text-[9px] font-bold uppercase tracking-wide text-white">{product.badge}</span>}<button onClick={() => onToggleSaved(product.id)} aria-label={`Save ${product.name}`} className="absolute right-2 top-2 rounded-full bg-white/90 p-2 text-black transition hover:bg-white"><Heart size={15} className={saved ? "fill-[#ff6b00] text-[#ff6b00]" : ""} /></button><button onClick={() => onAddToCart(product)} className="absolute bottom-0 left-0 right-0 hidden translate-y-full bg-black py-3 text-xs font-bold text-white transition group-hover:translate-y-0 sm:block">Quick add</button></div><div className="pt-3"><p className={`text-[10px] font-bold uppercase tracking-[.15em] ${dark ? "text-white/45" : "text-black/45"}`}>{product.category}</p><Link to={`/product/${product.id}`} className="mt-1 block text-left text-sm font-bold leading-5">{product.name}</Link><div className="mt-2 flex items-center justify-between"><span className="text-sm font-black">₹350</span><span className={`flex items-center gap-1 text-[10px] ${dark ? "text-white/65" : "text-black/55"}`}><Star size={11} fill="#ff6b00" className="text-[#ff6b00]" /> {product.rating}</span></div><button onClick={() => onAddToCart(product)} className={`mt-3 w-full py-2 text-xs font-bold sm:hidden ${dark ? "border border-white/25" : "border border-black/15"}`}>Add to bag</button></div></article>;
}
